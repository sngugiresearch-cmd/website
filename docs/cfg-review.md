# CFG article review — September 5, 2026

Both articles were rendered from canonical Markdown. The local integrity checker
passed source hashes, headings, canonical URLs, local links, TOC anchors, image
descriptions, RSS, and sitemap XML. Rebuilding without source changes produced
byte-identical outputs.

Chromium 141 through Playwright 1.56.1 checked each article at widths 390 and
1280, height 900, in dark and light themes: eight page/theme/viewport combinations.
All images decoded, contents links navigated, no page overflow remained, and no
JavaScript or HTTP errors were observed. The review fixed a nonwrapping date
line and added intrinsic figure dimensions for stable lazy loading. Tables,
code blocks, and large mobile figures scroll within their containers.

See `cfg-browser-review.json` for machine-readable results. Full-page screenshots
and standalone figure screenshots were inspected locally; they are not required
website assets. This review covers Chromium, not every browser or assistive tool.

The posts retain their original URLs/publication dates and archive links. The
current changes are intended for author review before deployment. Companion
code links target its `revise-cfg-articles` branch.

## Publication pass

The articles ship as **The Shape of a Lean Tactic** and **Lean Tactics Under
Constraint**. The companion is renamed **Tactic Grammar Lab**. Compact reference
lists are restored, with corrected Goedel-Prover-V2 attribution. Each article
includes a human-readable citation, downloadable BibTeX, companion-code URL,
and machine-readable citation metadata. Existing post URLs remain unchanged.
